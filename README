Se for a primeira vez:

  mkdir jQuery-Brasil
  cd jQuery-Brasil
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:ruanltbg/jQuery-Brasil.git
  git push -u origin master
      

Se ja existir?

  cd existing_git_repo
  git remote add origin git@github.com:ruanltbg/jQuery-Brasil.git
  git push -u origin master
  
comandos

sempre que fizer alterações seguir.
git status (mostra todas as modificações)
git add -A (add os arquivos novos)
git commit -am (Comita suas mudanças)
git pull origin master (baixa as ultimas atualizações
git status (verifica se precisa fazer algum merge nas alterações, caso precise, faca o merge ai repita do passo 3 em diante)
git push origin master (manda suas modificacoes adicionadas no commit para o servidor)